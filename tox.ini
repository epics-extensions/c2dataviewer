
[tox]
; List of Python enviroments to run in
envlist = py3


[testenv]

; Dependencies
deps = numpy
        psutil
        pvapy
        pyqtgraph == 0.12.4
        PyQt5
        blosc

; Coomands to run
commands = python -m unittest
